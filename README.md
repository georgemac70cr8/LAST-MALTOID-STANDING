# THE NINE — Vercel + Supabase (static)

This version has no Vercel Functions and no Netlify dependency. Vercel serves the static site; the browser uses the Supabase Data API with the project's publishable key.

The Supabase Publishable key is built into the static site, so players go straight to the game login screen. No browser setup is needed.

Supabase URL is preconfigured as https://dyhmelgjcmjkerqtvaaa.supabase.co
